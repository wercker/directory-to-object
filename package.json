{
  "name": "directory-to-json",
  "version": "1.0.0",
  "description": "Analyze a directory structure and return a `json` representation.",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test node test | colortape",
    "test-cov": "NODE_ENV=test istanbul cover test.js",
    "lint": "eslint ."
  },
  "repository": "wercker/directory-to-json",
  "keywords": [
    "directory",
    "analyze",
    "walk",
    "json"
  ],
  "license": "MIT",
  "dependencies": {
    "fs-walk": "0.0.1"
  },
  "devDependencies": {
    "colortape": "^0.1.1",
    "eslint": "^0.11.0",
    "istanbul": "^0.3.5",
    "tape": "^3.0.3"
  },
  "files": [
    "LICENSE",
    "index.js",
    "README.md"
  ]
}
